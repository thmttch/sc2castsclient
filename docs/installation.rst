============
Installation
============

At the command line::

    $ easy_install sc2castsclient

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv sc2castsclient
    $ pip install sc2castsclient